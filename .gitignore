# Environment / secrets
.env

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/

# Extracted data artifacts (CSV pulled by the SQL agent)
data/*.csv

# MLflow local tracking output (Phase 4 observability)
mlruns/
mlartifacts/
mlflow.db

# OS noise
.DS_Store

# Internal working docs (architecture notes, ADRs) — conveyed via README instead
docs/

# Internal master prompt — kept local, not for the public repo
MASTER_PROMPT.md
